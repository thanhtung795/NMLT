#include <stdio.h>
int main (){
	int n;
	printf ("nhap so co the chia het cho 3 = ");
	scanf ("%d",&n);
		if (n%3==0){
		printf ("%d la so chia het cho 3",n);
	}else {
	printf("%d la so khong the chia het cho 3 ",n);
	}

}
